const mongoose = require('mongoose');
const { BOOKING_STATUSES, PAYMENT_STATUSES } = require('../config/constants');

const bookingSchema = new mongoose.Schema({
  id: { type: Number, unique: true, index: true },
  property_id: { type: Number, required: true, index: true },
  guest_id: { type: Number, index: true },
  channel: { type: String, default: 'direct' },
  channel_booking_id: String,
  check_in: { type: String, required: true },
  check_out: { type: String, required: true },
  adults: { type: Number, default: 1, min: 0 },
  children: { type: Number, default: 0, min: 0 },
  infants: { type: Number, default: 0, min: 0 },
  nightly_rate: { type: Number, default: 0, min: 0 },
  subtotal: { type: Number, default: 0, min: 0 },
  cleaning_fee: { type: Number, default: 0, min: 0 },
  service_fee: { type: Number, default: 0, min: 0 },
  taxes: { type: Number, default: 0, min: 0 },
  gross_amount: { type: Number, default: 0, min: 0 },
  commission_percent: { type: Number, default: 0, min: 0 },
  commission_amount: { type: Number, default: 0, min: 0 },
  net_amount: { type: Number, default: 0, min: 0 },
  currency: { type: String, default: 'INR' },
  payment_status: { type: String, default: 'pending', enum: PAYMENT_STATUSES },
  payment_method: String,
  paid_amount: { type: Number, default: 0, min: 0 },
  pending_amount: { type: Number, default: 0, min: 0 },
  booking_status: { type: String, default: 'confirmed', enum: BOOKING_STATUSES },
  guest_message: String,
  special_requests: String,
  check_in_time: { type: String, default: '4:00 PM' },
  check_out_time: { type: String, default: '2:00 PM' },
  actual_check_in: String,
  actual_check_out: String,
  confirmed_at: String,
  cancelled_at: String,
  cancellation_reason: String,
}, { timestamps: { createdAt: 'created_at', updatedAt: 'updated_at' } });

module.exports = mongoose.model('Booking', bookingSchema);
